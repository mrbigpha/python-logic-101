def atm_withdrawal(amount):
    """
    โจทย์:
    ให้น้องๆ เขียนฟังก์ชันเพื่อคำนวณจำนวนธนบัตร (1000, 500, 100) ที่จะได้รับจากการถอนเงิน
    
    Input:
    - amount: int (จำนวนเงินที่ต้องการถอน เช่น 2600, 700, 255)
    
    Output:
    - คืนค่าเป็น string รูปแบบใดรูปแบบหนึ่งดังนี้:
    - "Invalid": ถ้า amount หารด้วย 100 ไม่ลงตัว
    - "1000: x, 500: y, 100: z": ถ้าถอนได้ (x, y, z คือจำนวนใบของแต่ละธนบัตร)
    
    เงื่อนไข:
    1. ถ้าจำนวนเงินที่ต้องการถอน (amount) ไม่หารด้วย 100 ลงตัว ให้ส่งค่ากลับเป็น "Invalid"
    2. ถ้าหารด้วย 100 ลงตัว ให้คำนวณจำนวนธนบัตรแต่ละชนิดและส่งกลับเป็นข้อความในรูปแบบ:
       "1000: x, 500: y, 100: z"
    
    ตัวอย่าง Input/Output:
    - atm_withdrawal(2600) -> "1000: 2, 500: 1, 100: 1"
    - atm_withdrawal(1000) -> "1000: 1, 500: 0, 100: 0"
    - atm_withdrawal(700) -> "1000: 0, 500: 1, 100: 2"
    - atm_withdrawal(255) -> "Invalid"
    
    คำใบ้:
    - ใช้ // (Floor Division) เพื่อหาจำนวนธนบัตร
    - ใช้ % (Modulo) เพื่อหาเศษเงินที่เหลือ
    - ใช้ if-else เพื่อตรวจสอบเงื่อนไข "หารด้วย 100 ลงตัว" ก่อนเริ่มคำนวณ
    """
    
    # --- เริ่มเขียนโค้ดด้านล่างนี้ ---
    raise NotImplementedError
    

    # --- สิ้นสุดการเขียนโค้ด ---

if __name__ == "__main__":
    test_amount = 2600
    print(f"Withdraw {test_amount} : {atm_withdrawal(test_amount)}")
