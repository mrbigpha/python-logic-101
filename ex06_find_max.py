def find_max(a, b):
    """
    โจทย์:
    ให้น้องๆ เขียนฟังก์ชันเพื่อหาค่าที่มากกว่าระหว่างตัวเลขสองตัว (a และ b)
    
    Input:
    - a: int หรือ float (ตัวเลขแรก)
    - b: int หรือ float (ตัวเลขที่สอง)
    
    Output:
    - คืนค่าเป็น int หรือ float
    - คืนค่า a ถ้า a มากกว่า b
    - คืนค่า b ในกรณีอื่นๆ (b มากกว่า หรือ b เท่ากับ a)
    
    ตัวอย่าง Input/Output:
    - find_max(10, 20) -> 20
    - find_max(50, 10) -> 50
    - find_max(30, 30) -> 30
    
    เงื่อนไข:
    - ถ้า a มากกว่า b ให้ส่งค่ากลับเป็น a
    - ถ้า b มากกว่าหรือเท่ากับ a ให้ส่งค่ากลับเป็น b
    """
    
    # --- เริ่มเขียนโค้ดด้านล่างนี้ ---
    raise NotImplementedError
    

    # --- สิ้นสุดการเขียนโค้ด ---

if __name__ == "__main__":
    x, y = 10, 20
    print(f"Max of {x} and {y} is: {find_max(x, y)}")
